# User Journey (Assistant role) — 2026-09-14 05:11 (harness-driven fallback run)

**Driver note:** Playwright MCP was **not connected** in this session (no
`mcp__playwright__*` tools available), so per §0a this run used the repo's
own scripted fallback instead of the interactive browser walkthrough:

```
cd .claude/skills/beta-test && npm install && node runner.mjs --only role
```

**Consequence:** this run exercised the `role` journey suite in
`.claude/skills/beta-test/journeys/05-roles.mjs`, which covers a slice of
Phase 1 (role-boundary audit) and Phase 2 (job the role exists for) across
**all three roles** (assistant, librarian, admin) in one pass — not this
file's Assistant-only, section-by-section script. **Phase 2.5 (server-side
API sweep), Phase R (responsive), Phase S (session lifecycle), and Phase F
(friction sweep) were NOT executed** — the harness does not drive raw
`fetch()` probes, resize the viewport, or run the no-reload friction checks
this playbook specifies. Re-run with Playwright MCP connected for full
coverage of those phases.

Environment stood up for this run: local Postgres started (was not running),
scratch/dev databases created (`library_system_playbook` for the manual §1
reseed steps used to validate setup; the harness itself independently
created and seeded its own `library_system_beta` on ports 4100/5273 — its
usual dedicated ports, not 4300/5374, since the interactive §1 port-picking
step was not applicable to a scripted run), `server/.env` created (did not
exist previously) with a local `DATABASE_URL`.

## Harness result

**Verdict: PASS** — 3/3 journeys, 0 failed, 0 blocked, 76s. Full report:
`.claude/testings/run-2026-09-14T05-11/report.md`.

| Journey | As | Steps | Result |
|---|---|---|---|
| An assistant can work the desk but not run the library | assistant | 9/9 | PASS |
| A librarian runs the collection but not the staff list | librarian | 5/5 | PASS |
| An administrator manages staff accounts | admin | 7/7 | PASS |

### Assistant journey — step detail (maps loosely to this playbook's Phase 1 / Phase 2 ids)

| Step | Maps to | Result |
|---|---|---|
| They are told which role they hold | u-p1-nav (footer role text) | PASS |
| User Management is not offered to them at all | u-p1-nav / u-p1-usermgmt | PASS |
| They can still read the catalogue | u-p1-catalog (read) | PASS |
| But they are not offered Add, Edit or Delete | u-p1-catalog (absent controls) | PASS |
| They can check a book out — that is their job | u-home-borrow / u-circ-* | PASS |
| Fines are visible but not collectable by them | u-p1-fines | PASS |
| Fine policy is readable but locked | u-p1-settings | PASS |
| And the shelf lists cannot be edited | u-p1-settings (lookups) | PASS |
| Tidy up: return the book they borrowed | cleanup | PASS |

No failures, no console errors (clean per the harness's console check, which
treats uncaught exceptions and 5xx as failures and 4xx from intentional
wrong-password/suspended/duplicate-hold probes as expected).

## Bootstrap (as Admin)

Not applicable — the harness signs each journey's persona in directly
(`personas.mjs`); the B0–B5 bootstrap/hand-over sequence in this playbook is
specific to the interactive Playwright-driven run and was not performed.

## Phase 1 — Role-boundary audit

Partially covered via the harness's assistant journey (see table above:
nav filtering, catalog/fines/settings read-only enforcement). **Not
individually verified:** the full nine-item Main Menu enumeration, Dashboard
Quick Actions, Circulation/Reservations panel contents, Reports tabs/export,
and Labels & Barcodes — the harness journey does not visit every section this
playbook's `u-p1-section-*` table lists.

## Phase 2 — The job the role exists for

Partially covered (checkout + return, per above). **Not covered:**
scan-barcode Enter-key flow (`u-circ-scan-checkout`, `u-circ-refocus`),
renew, hold placement/duplicate/fulfil/cancel, suspended-member refusal
detail, empty-search state, exports, labels, appearance cross-session
propagation (`u-appearance-global`).

## Phase 2.5 — Server-side authorization sweep

**Not executed.** No `fetch()` probes were run from within a session; the
harness's role coverage is UI-only. This is the phase most worth re-running
with Playwright MCP, since it is this playbook's stated "boundary that
actually matters."

## Phase R — Responsive

**Not executed.**

## Phase S — Session & auth lifecycle

**Not executed.** Note: the harness's own tidy-up steps (returning the
borrowed book, removing the test clerk) confirm session-scoped writes work
end-to-end within a single persona session, but no reload/sign-out/
disabled-mid-session/role-change-mid-session checks were run.

## Phase F — Friction sweep

**Not executed.**

— Items: 3 journeys (21 sub-steps) run · 21 PASS · 0 FAIL · 0 BLOCKED · 0 NEEDS-HUMAN · 0 SKIPPED
— Phases fully executed: partial Phase 1, partial Phase 2 (assistant slice)
— Phases not executed: Phase 2.5, Phase R, Phase S, Phase F, and the remainder of Phase 1/2 not covered by the scripted journey

## Restricted-user friction observations

One polish-level note surfaced by the harness (from the **librarian**
journey, not the assistant one, but recorded here since it bears on the
`u-appearance-global` question this playbook asks): theme persisted across
reload as `rgb(221, 227, 208)`. No friction specific to the Assistant
persona was flagged by the harness this run.

→ Suggestion: none required from this run's findings; a targeted
`u-appearance-global` check across two tabs (Assistant + Admin) is still
outstanding and is the best way to resolve whether the shared-appearance
behavior is a UX concern.

"No friction noted this run" beyond the above — no P0/P1 issues surfaced.

## Bugs found

No bugs found this run.

(Caveat: given the phases skipped above — most importantly Phase 2.5's
server-side authorization sweep — this is not a claim that no bugs exist,
only that none were found within the harness's UI-level role coverage.)

## Recommendation

Re-run this playbook with Playwright MCP connected to get full coverage,
particularly Phase 2.5 (API-level authorization sweep) and Phase S
(mid-session disable/role-change), which this harness-driven run could not
exercise.
